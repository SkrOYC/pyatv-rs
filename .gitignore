# Rust
# Cargo.lock IS committed: this workspace ships a binary (cli/atvremote), so a reproducible
# dependency graph matters more than letting library consumers re-resolve.
/target

# Devenv
.devenv*
devenv.local.nix
devenv.local.yaml

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml
